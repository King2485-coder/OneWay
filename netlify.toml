[build]
  base = "OneWay/server/netlify-site"
  command = "npm run build"
  publish = "dist/client"

[[headers]]
  for = "/*"
  [headers.values]
    X-Content-Type-Options = "nosniff"
    X-Frame-Options = "DENY"
    Referrer-Policy = "strict-origin-when-cross-origin"
